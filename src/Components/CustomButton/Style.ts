import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    height: 50,
    borderWidth: 1,
    borderColor: 'gray',
    marginHorizontal: 16,
    borderRadius: 8,
    justifyContent: 'center',
  },
  title: {
    textAlign: 'center',
    fontSize: 20,
    color: 'green',
  },
});
